[pytest]
pythonpath = src
testpaths = tests
# Default to not running GPU tests unless explicitly asked? 
# Or just rely on markers.
markers =
    gpu: tests that require a real GPU
    slow: tests that take a long time
addopts = --strict-markers
